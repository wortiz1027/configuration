server:
  port: 7072

management:
    server:
      port: 6172

spring:
  application:
    name: server-gateway

  cloud:
    gateway:
      routes:
      - id: srv-bills
        uri: http://${BILLS_HOST:localhost}:8081/
        predicates:
        - Path=/b/**
      - id: srv-clients
        uri: http://${CLIENTS_HOST:localhost}:8082/
        predicates:
        - Path=/c/**
      - id: srv-products
        uri: http://${PRODUCTS_HOST:localhost}:8083/
        predicates:
        - Path=/p/**

logging:
  level:
    org.springframework.web: DEBUG
  pattern:
    console: "%d{yyyy-MMM-dd HH:mm:ss a} [%t] %-5level %logger{36} - %msg%n"
    file: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
